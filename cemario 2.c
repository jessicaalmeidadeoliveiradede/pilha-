#include <stdio.h>
struct pilha{
	int numero;
	char pilha;
}typedef pilha;
void pilha_numero (pilha*pilha, int numero){
pilha_numero = (pilha*)malloc(sizeof(numero));
};
void inserir(pilha *pilha){
  pilha->numero=1; 
  pilha->numero=6;
 pilha->numero=7;
  pilha->numero=9;
    
  
};
void imprimar(pilha *pilha){
  pilha->numero=9;
  pilha->numero=7; 
  pilha->numero=3;
   pilha->numero=2;
    pilha->numero=1;
    
};
void imprimar(pilha *pilha){ 
  pilha->numero=1;
   pilha->numero=9;
    pilha->numero=6;
  pilha->numero=7;
  pilha->numero=6;
    
};
void imprimar(pilha *pilha){ 
  pilha->numero=8;
   pilha->numero=1;
    pilha->numero=6;
  pilha->numero=7;
  pilha->numero=6;
    
};
void remover(pilha*pilha){
pilha->numero=(pilha vazia);
};
int main{
  pilha*numero;
  int numero;
while(numero != 0){
  system("numero");
  printf("PILHA \n");
  printf("adiciona os numero na pilha "1,2,3,7,9);
  printf("impreme mais alguns numero  na pilha",9,7,3,2,1;
printf("e depois remova os nnumero na pilha",pilha vazia;

scaf("%pilha",numero);
  switcha(numero){
    case 1
      printf("quais são os numero que vc deseja adicionar na pilha");
      scaf("%pilha",numero ),
        pilha(pilha,numero);

      printf("vamos adicionar")
        adicionar(PILHA);
        getcha();
          getcha();
  };
    case 2
      printf("quais são numero que quer impremir");
     scaf("%pilha",numero );
      pilha(pilha,numero);
      impremir(pilha);
        getcha();
          getcha();
  case 3
    printf("vamos remove a pilha");
    scaf("%pilha",numero);  
    pilha(pilha,numero);
    remove(pilha);
     printf("pilha fazia\n")
       getcha();
        getcha();
     
    
}brea;
	
	

	
		